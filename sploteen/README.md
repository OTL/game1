# スプロティーン

1台のタブレットを2人が向かい合わせで使う、対戦型ぬりぬりゲームです。
対戦画面は three.js による3D表示です(第11ラウンド)。`sploteen/index.html` をブラウザで開くだけで遊べます。

three.js は `sploteen/vendor/` に同梱しているのでインストールは不要ですが、ESモジュールとして読み込むため
**http(s) 経由で開く必要があります**(GitHub Pages で公開しているURLならそのままでOK。手元で開くなら
`python3 -m http.server` などを使ってください)。`file://` で直接開いた場合はブラウザがモジュールを読めないので、
自動的に従来の2D表示(`render2D`)にフォールバックします。WebGLが使えない端末でも同様に2D表示になります。

## 遊び方

1. タイトル画面で「ふたりで たいせん」(通常の2人対戦)か「ひとりで れんしゅう (CPU)」(1人でCPU相手に練習)を選ぶ
2. 武器選択画面で、各プレイヤーがメイン武器(5種類)・サブウェポン(4種類)・スペシャル(3種類)をそれぞれ1つずつ選ぶ。画面上部の「アバター」ボタンから自分のキャラも作れる
3. 「じゅんび OK」を両者(CPUモードでは自分だけ)が押すとカウントダウンの後、1ラウンド90秒の対戦が始まる
4. 90秒のあいだにマップを自分の色でたくさん塗った方がそのラウンドの勝ち(勝敗の基準は最後まで「塗り割合」)
5. 相手を攻撃して倒す(HPを0にする)こともでき、倒すたびに大きな飛沫と「たおした!/やられた!」の演出が出るが、ラウンドの勝敗そのものは塗り割合で決まる
6. **3ラウンド制・2本先取**でシリーズ優勝が決まる(1ラウンド目を落としても2ラウンド目・3ラウンド目で挽回できる)
7. ラウンドに負けた側は、次のラウンド開始時にスペシャルゲージが50%たまった状態でスタートできる(ゴムバンド調整)

### ひとりで れんしゅう (CPUモード)

- タイトルで「ひとりで れんしゅう (CPU)」を選ぶと、P2(うえ)がCPU操作になる
- 武器選択画面のP1(した)側に「CPU(うえ)の ぶきを えらぶ」という武器・サブ・スペシャルの3行と「CPU の つよさ」ボタン(よわい/ふつう/つよい)が出る。P1は自分の側からCPUの装備・強さの両方を選べる(P2側の回転表示にもそのままCPUの選択が反映される)
- 強さは `CONFIG.BOT_SKILL` に反映される: よわい=0.35 / ふつう=0.6 / つよい=0.85
- CPU側の「じゅんび」は自動でON扱いになり、P1が「じゅんび OK」を押すだけで対戦が始まる
- 既存のURLパラメータ `bot2=1` / `bots=1` はメニューを通しても引き続き有効(タイトルで「ふたりで たいせん」を選んでも、URLで指定した側はCPU操作になる)

### 画面の見かた

- **対戦画面は3D(第11ラウンド)**。マップ・壁・キャラ・インク・エフェクトを three.js で描き、上下端の薄いHUD帯(各48px)を除いた領域いっぱいに表示する。詳しくは「[3D表示(第11ラウンド)](#3d表示第11ラウンド)」を参照
- HPバー・状態異常アイコン・「たおした!/やられた!」・スペシャル発動の文字だけは、3Dの上に重ねた2Dキャンバスに描いている(文字はこのほうが読みやすいため)
- 操作パッド(移動スティック・エイムパッド)はマップの上に重ねる半透明のオーバーレイになっており、固定UIとしては存在しない。自分側の縁から画面高さの30%が操作ゾーンで、うっすらとした境界線と、触れると消える点線の輪が目印
- スペシャルボタン(SP)は自分側の縁・エイム側の角に配置。サブボタンはその隣に同じ大きさで配置される。どちらもHUD帯(48px)より内側(マップ側)に置いてあり、HUDのバーや文字を隠さない
- **サブ/スペシャルのボタンは「下からインクがたまっていく」ゲージそのもの**(第10ラウンド)。ための進み具合がボタンの下から色づいて上がっていき、満タンになると縁が脈打つように光る
  - スペシャルボタン: ゲージ(`sp / SPECIAL_GAUGE_MAX`)の割合ぶん、黄色が下から満ちる。満タン・発動中は光って脈打つ
  - サブボタン: 「クールダウンの進み」と「インクの足り具合(`ink / inkCost`)」のうち**少ない方**が水色で下から満ちる。インク不足で止まっているときだけ色が灰色に変わるので、待っているのが時間かインクかが区別できる
- HUDには武器アイコン・HPバー・インクバー・HP数値(または復活までの秒数)・その試合の撃破数(「たおした N」)を表示。スペシャルゲージのバーはSPボタン自身がゲージになったため削除した(二重表示だった)
- P2(うえがわ)のHUD・パッドは180度回転して表示され、P2から見て正しい向きで文字が読める
- 画面中央上下の数字が残り時間(片方は回転して表示)。残り10秒は画面が赤く点滅し、大きなカウントダウンが出る
- 横持ちでも同じ縦積みレイアウトのままマップが中央に収まる(左右分割はしない)。武器選択画面だけは横持ち時にメイン/サブ/スペシャル/CPUパネルを横並びに組み替え、縦スペースを節約する

### 3D表示(第11ラウンド)

スプラトゥーンの画面を参考に、対戦画面を斜め見下ろしの3Dに作り直しました。**ゲームのロジック(当たり判定・塗り・移動・CPU・勝敗判定)は2Dのときと同一**で、変えたのは見せ方だけです。`SPLOTEEN.simulate()` によるヘッドレス対戦も一切影響を受けません。

#### カメラは「平行投影(Orthographic)」

1画面を上下から2人で共有するゲームなので、カメラの置き方が公平性に直結します。

- **遠近法(Perspective)で前後に傾けると不公平**: カメラに近いP1側が大きく、遠いP2側が小さく写ってしまう
- そこで**平行投影のカメラをP1側の斜め上(真上から42度)に置いた**。平行投影は距離で大きさが変わらないため、斜めに見下ろしても**上半分と下半分がまったく同じ縮尺**で写る
- 画面の右=ワールド+X、画面の上=ワールド-Y に保っているので、ツインスティックの操作感(画面に対してどちらへ動くか・撃つか)は2Dのときと完全に同じ
- 高さ(+Y)は画面の上方向に立ち上がるので、壁もキャラも「立っている」ように見える
- マップ全体(床下からキャラの頭上まで)が必ず画面に収まる倍率を `fitCamera3D()` が画面サイズごとに計算する。縦持ち・横持ちのどちらでも全体が見える

#### 塗りの表示: 床をタイルに割って差分だけ転送する

塗り自体は今までどおりオフスクリーンの2Dキャンバス(`g.inkCanvas`、1344×2016px)に丸を重ねて描いています。これを毎フレームまるごとGPUへ送ると重いので、

- 床を**12セル(168px)角のタイル24枚**に割り、それぞれ独立したテクスチャにした
- 塗る関数(`drawInkBrush` / `drawInkEllipse` / `paintSector`)から `markInkDirty()` を呼び、**塗られたタイルだけ**を作り直して転送する
- タイルのテクスチャにはミップマップを作らない(ほぼ等倍で見るので画質は変わらず、毎フレームの生成コストが消える)

この2つで、塗りの転送コストはほぼ無視できる水準になりました(ソフトウェアレンダリングでの計測で、転送を完全に止めた場合との差が5%以内)。

#### キャラクター

アバターの設定(しゅぞく・かみがた・かみのいろ・め・はだ・ぼうし・ふくのいろ)から、球・カプセル・円柱などの基本形を組み合わせて3Dモデルを毎回組み立てます(`buildAvatar3D`)。設定か武器が変わったときだけ作り直します。

- 体の正面がエイム方向を向く。足元にチーム色のインク跡と方向を示す矢印を出す
- 背中にチーム色のインクタンクを付けているので、後ろ姿でもどちらの色か分かる
- 武器は5種類それぞれの形(シューター=銃、ローラー=ドラム、チャージャー=長い銃身、バケツ=桶、フデ=筆)を手元に持つ
- ローラーの転がしモード中は前のドラムが出る/離すと消える、被弾で白く光る、無敵中は足元の白い輪が明滅する、といった2Dのときの演出はすべて3Dに置き換えて残している

#### 選択画面・VS画面・リザルト画面

武器選択画面のアバタープレビュー、VS画面、ラウンド結果・優勝画面のアバターも、同じ3Dモデルを小さな専用レンダラで描いて各canvasへコピーしています(ゆっくり回転します)。武器カード・ボタン・塗り割合のバーにも厚みのある影と傾きを付けて、画面全体の見た目を揃えました。

#### フォールバック

`import('./vendor/three.module.min.js')` に失敗した場合(`file://` で開いた・WebGLが使えない)は `R3D.state='failed'` になり、従来の2D描画(`render2D`)にそのまま切り替わります。遊べなくなることはありません。

### リセットボタン(第7ラウンドで追加)

- 画面左端の中央(上下のHUD帯の間、操作パッドの帯からは離れた位置)に直径40px程度の半透明の丸い「↺」ボタンを常設。誤タップしにくいよう半透明・パッド帯の外に配置している
- タイトル画面では非表示。武器選択・カウントダウン・対戦中・ラウンド結果・優勝画面では表示される
- タップすると確認オーバーレイ「さいしょから やりなおす?」が開く。文言はP1向き(下)・P2向き(上、180度回転)の両方に表示し、「はい」「いいえ」ボタンは中央共通(どちらのプレイヤーが押してもよい)
- 「はい」: 進行中のシリーズを中断し、スコア・ラウンド数・塗りをすべて初期化してタイトル(ふたりで たいせん / ひとりで れんしゅう の選択)へ戻る
- 「いいえ」: オーバーレイを閉じて続行(ゲーム状態は変化しない)
- `simulate()` によるヘッドレス対戦には一切関与しない(画面のボタン・オーバーレイのみの機能)

### サブ/スペシャルのエフェクト(第10ラウンドで強化)

「いま何が起きたのか」がひと目で分かるよう、サブとスペシャルに共通のエフェクト(広がる輪 `ring` / 直線の閃光 `streak` / しぶき)を追加しています。いずれも見た目だけの処理で、`g.headless`(`simulate()` のヘッドレス実行)では何も生成しないため勝敗計算には影響しません。

- ボム(スプラッシュ/クイック): 投げた瞬間に足元の輪、着地後は**爆発範囲を点線の輪で予告**(起爆が近いほど濃く太くなる)、爆発時は爆発半径ぴったりの白い輪+外へ広がる自色の輪+しぶき+軽い画面ゆれ
- スプリンクラー: 設置位置に塗り範囲ぶんの輪と白い内輪、しぶき
- スプラッシュシールド: 張った壁の線が閃光として光り、中心から輪が広がる
- スペシャル発動: 足元から三重の輪が広がり、しぶきが飛び、画面がゆれる。中央の発動演出はスペシャルの絵文字+名前を帯つきで大きく表示(0.45秒、出た瞬間だけ少し大きく出る)
- せんせいストライク: 着弾の帯を**実際の当たり幅**の閃光で表示し、こおり範囲の縁も細い線で表示。着弾線上にしぶきが飛ぶ
- むらさめウツボ: 範囲内に輪としぶきが降り注ぎ続けるので、どこが範囲内かが分かる
- マンタりんぶ: 走った跡に水しぶき、引き寄せ範囲は内向きに縮む輪で表示、体当たりの瞬間は大きな白い輪+しぶき+画面ゆれ
- しぶきの粒には白い縁をつけており、自分の色で塗った床の上でも埋もれない

### 倒したときの演出

- 大きな飛沫(相手色でまとまった範囲を塗る)と、画面が短く軽く揺れる演出
- やられた側のキャラ付近に赤系で「やられた!」、倒した側の色で「たおした!」のポップアップが浮かんで消える。自分側HUDにも同じ内容のトーストが一瞬出る
- 復活まで残り3・2・1秒はキャラが死亡した位置に大きな数字で表示され、自分側HUDのHP欄も「ふっかつ N」に切り替わる
- 被弾した瞬間はキャラの体が一瞬白く光り、相手色の飛沫が付く
- ラウンド結果画面には塗り割合の他に「P1 たおした:x / やられた:y」「P2 たおした:x / やられた:y」も表示される(勝敗の判定基準は変えず、塗り割合のまま)

## 操作(ツインスティック)

タブレットは1画面を2人でタッチして遊びます。各自の操作ゾーンは自分側の縁から画面高さの約30%(マップに重なる半透明オーバーレイ)。

| ゾーン | 操作 |
| --- | --- |
| ゾーンの左半分(移動スティック) | 指を置いてドラッグした方向・距離で移動(フローティングスティック) |
| ゾーンの右半分(エイムパッド) | 触れている間、ドラッグ方向に武器を撃ち続ける(チャージャーはため続ける)。指を離すと止まる。方向がなければ今向いている方向に撃つ |
| SPボタン(エイム側の角の丸ボタン) | スペシャルゲージが満タンのときに発動 |
| サブボタン(SPボタンの隣) | タップでサブウェポンを使用(インクを消費、使用後1.0秒クールダウン)。ボタンは下からたまるチャージゲージになっている。投げる/設置する向きは最後にエイムした方向(なければ移動方向) |

同時に4本指まで独立して追跡できます。指がゾーンの外に出ても追跡は続きます。

### PC用フォールバック(開発確認用)

| プレイヤー | 移動 | 狙い・発射 | スペシャル | サブ |
| --- | --- | --- | --- | --- |
| P1(した) | W/A/S/D | マウス移動で狙う、左クリック長押しで発射 | E キー | R キー |
| P2(うえ) | 矢印キー | I/J/K/L で8方向に発射(押している間撃つ) | O キー | U キー |

## メイン武器(5種類)

各プレイヤーは対戦前に5種類から1つを選びます。パラメータは `index.html` 先頭の `WEAPONS` オブジェクトに集約されており、実行中に書き換えると次の対戦から反映されます。値の「(人間)」はプレイヤー操作専用のインクコスト(下記「インクのルール」参照)、それ以外はCPU・人間共通です。

### 🔫 シューター「バランスがた。まよったらこれ」

- 連射(毎秒8発)、1発35ダメージ(3発で撃破)、射程230px(第8ラウンドで280→230。マップ幅672pxの3分の1程度)の近距離武器
- 移動速度は他武器よりやや速い(`moveSpeedMul=1.14`)
- 着弾の塗り半径2.1セル(第11ラウンドで2.0→2.1)。1発の消費インクは6(人間は2)
- 既定サブ: スプラッシュボム / 既定スペシャル: せんせいストライク

### 🖌️ ローラー「おもくて おそい。ボタンを おしながら ころがして ぬる。ひいて たおす」

- 移動速度は全武器で最も遅い(`moveSpeedMul=0.78`)。相手インクの上ではさらに大きく減速する(`enemyInkMul=0.44`)
- **転がしモード(第7ラウンドで変更)**: エイムパッド(fire入力)を押している間だけ「転がしモード」になり、押しながら基準速度の30%以上(`ROLL_PAINT_MIN_SPEED_MUL=0.3`)で動いている間だけ進行方向の幅5セルを塗れる(`rollWidth=5`)。塗り跡は移動距離に応じて幅が波打ち、両端がギザギザに見える。**エイムパッドを押していない時や、押していても止まっている時は塗らない**(ずっと自機の周りを塗り続けることはできない)。転がしモード中はキャラの前に短いローラーの棒を表示し、離すと消える
- **転がしのインク消費(第8ラウンドで変更)**: 人間プレイヤーの転がしは「転がして進んだ距離(1pxにつき`rollCostPerPx=0.045`)」+「新しく塗ったセル数(1セルにつき`rollCostPerCell=0.05`)」を消費する。距離の分があるので自陣の色の上を転がしていてもタンクは減り、転がしている間は回復も止まる(消費のたびに`INK_REGEN_DELAY`の待ち時間がリセットされる)。押しっぱなしで転がし続けると満タンから約10秒で空になり、エイムパッドを離して0.5秒待つと地形に応じた回復が始まる。インクが0になると転がしても塗れない
- 接触ダメージ60。転がしモード中(押しながら、基準速度の50%以上で動いている時)だけ接触キルが発動する(待ち伏せしての即死コンボ・待機中の轢き逃げを防止)
- エイムパッドを短くタップ(押してから`TAP_MAX_DURATION`(既定0.25秒)未満で離す=振り)すると前方扇状に7発のインク飛沫、1発55ダメージ。それより長く押した場合は転がしモードのままになり、離しても振りは出ない。振りの消費インクは34(人間は20)
- PC用フォールバックでも同じ理屈: P1はマウス左ボタン長押し、P2はIJKL長押しが転がしモードの入力になる(短く離せば振り)
- CPU(ボット)は、塗るために移動する間はfireを押し続けて転がしモードを維持し、振りの間合いに入った時だけ一瞬離してから短くタップし直して振りを出す(判断ロジック自体は変えず、fire入力の出し方だけ読み替えている)
- 既定サブ: クイックボム / 既定スペシャル: マンタりんぶ

### 🎯 チャージャー「ためてうつ。とおくからねらえ」

- エイムパッドを押している間ため(最大1.2秒)、ため中は移動速度が半分に
- 離すと直線ビーム。フルチャージで射程はマップ縦の70%、ダメージ100(一撃)。ため不足でも最低45%は保証
- 通過した線の塗り半径1.4セル
- 消費インクは基礎15+チャージ割合×35(人間は基礎6+チャージ割合×12)。ため始めるにはインクが8以上必要
- 既定サブ: スプリンクラー / 既定スペシャル: せんせいストライク

### 🪣 バケツ「ひろく ぬる。かべを こえて とどく」

- エイムパッドを触れている間、0.48秒ごとに1発。前方に扇状(半角20度、射程230px)の大きなかたまりを塗る。**壁を無視して届く**(壁越しに塗り・攻撃ができる唯一の武器)
- 近距離(8セル以内)の敵に70ダメージ。1発の消費インクは19(人間は8)
- 移動速度はやや速め(`moveSpeedMul=1.06`)
- 既定サブ: スプラッシュシールド / 既定スペシャル: むらさめウツボ

### 🖌 フデ「かるくて はやい。れんぞくで ふりはらう」

- 移動速度は全武器最速(`moveSpeedMul=1.45`)。相手インクの上での減速は他武器より緩やか(`enemyInkMul=0.75`)
- 移動しているだけで幅2.55セルを塗れる(`trailWidth=2.55`。第9ラウンドで2.0→2.81、第11ラウンドで2.81→2.55。接触ダメージなし)。ローラー(幅5セル・接触キル)との違いは「速いが倒せない/倒されにくい」対「遅いが轢ける」
- エイムパッドを押している間0.22秒ごとに自動連続で振りはらい、1発10ダメージ×4発、射程150px。1振りの消費インクは6
- 既定サブ: クイックボム / 既定スペシャル: マンタりんぶ

各武器には日本語の一言説明を用意し、選択画面に表示しています。

## サブウェポン(4種類)

メインとは別に、対戦前にサブウェポンを1つ選びます。使用にはインクが必要で、足りないとサブボタンのゲージが灰色のまま満タンになりません。使用後は共通で1.0秒のクールダウンがあります。投げる/設置する向きは「最後にエイムした方向(なければ移動方向)」で、飛距離は固定です。パラメータは `index.html` の `SUBS` オブジェクトに集約されています。

| サブ | 絵文字 | 説明 | 主なパラメータ |
| --- | --- | --- | --- |
| スプラッシュボム | 💣 | なげて じばくはつ。ひろく ぬれる | 飛距離200px、着地1.0秒後に爆発、半径4.9セル、中心100/端40ダメージ、インク30 |
| クイックボム | ⚡ | あたると すぐ ばくはつ。れんとうできる | 飛距離160px、着弾で即爆発、半径3.8セル、中心35/端15ダメージ、インク20 |
| スプリンクラー | 🌀 | おいとくと じどうで ぬってくれる | 飛距離150px、設置後12.5秒間、周囲半径5.8セルに毎秒6.5回ランダムに飛沫を撒いて塗る(ダメージなし)。同時に1個まで、相手の弾で壊せる(HP50)。インク28 |
| スプラッシュシールド | 🛡 | かべを つくって まもる | 前方3セルに幅8セルのインクの壁を8秒間設置。相手の弾・ビームを止める(自分の弾は通る)。相手は壁を通れないが、自分は通れる。壁の下は半径3.6セルの帯で自分色に塗られる。インク35 |

第11ラウンドで4種ともまとめて強化しました(下の「第11ラウンド: サブウェポンの強さをメインと比べて測り直した」を参照)。

- 既定サブ: 選択画面の初期値はP1がシューター+スプラッシュボム、P2がローラー+クイックボムで、CPU/自動対戦の既定サブは武器ごとに shooter→splashbomb / roller→quickbomb / charger→sprinkler / bucket→shield / brush→quickbomb(`WEAPONS[key].defaultSub`)
- サブウェポンの使用回数は `stats.subsUsed` に記録され、`simulate()` の戻り値 `subs:[n1,n2]` で取得できる

## スペシャル(3種類)

武器から独立した必殺技です。武器選択画面3行目の「スペシャル」で、メイン・サブとは別に1つ選びます。ゲージは全武器共通の `SPECIAL_GAUGE_MAX`(既定1800)で統一されており、自分の色で「新しく塗ったセル数」だけたまります(塗り直しは加算しない)。倒すと `SPECIAL_KILL_BONUS`(ゲージ比15%)ぶん追加でたまります。パラメータは `index.html` の `SPECIALS` オブジェクトに集約されています。

### ⚡ せんせいストライク

まっすぐ とおくまで こうげき。よけられても こおらせる。

- 発動から0.6秒の予告時間(赤い細い線でキャラの向いている方向を予告)ののち、その方向へマップ端まで届く幅5セルの一撃を放つ
- 直撃した敵は100ダメージ(ほぼ即倒し)。直撃を免れても、範囲の縁(直撃の外側+3セル、幅11セル相当)にいれば「こおり」状態になる
- 発動中もキャラは自由に動ける

### 🐍 むらさめウツボ

まえの ひろい はんいに ウツボの ぐんれいが ふりそそぐ。

- 発動から5秒間、キャラの前方・幅12セル×奥行き16セルの範囲にウツボの群れが降り注ぐ(範囲はキャラの向きに追従)
- 範囲を毎秒40%ずつ塗りつぶし、範囲内の敵に毎秒35ダメージ。当たった敵は「はっこう」状態になる
- 発動中も自分は自由に動ける

### 🪁 マンタりんぶ

むてきで マンタに のり、たいあたり と インクだんで あばれる。

- 発動から3秒間マンタに乗る。**無敵**・移動速度×1.3・幅6セルの跡を塗る
- 体当たり(接触)で敵に85ダメージ。半径6セル以内の敵を毎秒少しずつ引き寄せる
- エイムパッドで大きなインク弾(半径3セル塗り、42ダメージ、毎秒2発)を投げられる
- 接触・インク弾どちらで当てても「あぶり」状態を付与する

### 状態異常

| 状態 | 付与する特殊 | 効果 |
| --- | --- | --- |
| こおり ❄️ | せんせいストライク(縁の範囲) | 2秒間動けない(撃つことはできる) |
| はっこう 🫧 | むらさめウツボ | 4秒間、移動速度×0.6・被ダメージ×1.5 |
| あぶり 🔥 | マンタりんぶ(接触/インク弾) | 3秒間、毎秒12ダメージ |

「こおり」「はっこう」「あぶり」はキャラの頭上に小さなアイコンと残り時間のリングで表示されます。復活すると状態異常はすべて解除されます。`simulate()` の戻り値 `statusHits:[n1,n2]` で、各プレイヤーが状態異常を新規に与えた回数を取得できます。

## インクのルール

インクタンクは共通で100。地形の上にいると移動速度が変わります(自分の色の上は1.3倍、相手の色の上は0.55倍。ローラーは相手色の上でさらに重く感じる0.44倍、フデは逆に軽い0.75倍)。

### 人間プレイヤー(タッチ操作)のインク経済

CPUとの差別化として、人間が操作するプレイヤーには次の専用ルールが適用されます(CPUには一切適用されません)。

- 地形によるインク回復速度: **自陣の色の上+35/秒・未塗りの上+8/秒・相手の色の上+0/秒**
- 撃つ・振る・ため中・サブを使う、いずれの消費でも「最後にインクを消費してから **0.5秒**(`INK_REGEN_DELAY`)」は回復が始まらない
- 武器ごとの人間専用インクコスト: シューター1発2 / ローラー振り20 / チャージャー基礎6+チャージ割合×12 / バケツ1発8
- ローラー・フデは「移動しているだけで塗れてインクを一切使わない」性質があるため、両武器の移動塗りには専用コストがあります
  - ローラーの転がし(第8ラウンドで変更): 進んだ距離1pxにつき0.045(`rollCostPerPx`)+ 新しく塗ったセル1つにつき0.05(`rollCostPerCell`)。この消費は0.5秒の回復待ちをリセットするので、**転がしている間はインクが回復しない**(自陣の上でも減る)。転がしっぱなしで満タンから約10秒
  - フデの走り塗り: 新しく塗ったセル1つにつき0.06(`brushCostPerCell`。自陣の上を通っても新規扱いにならず無料)。この消費は0.5秒の回復待ちをリセットしないので、自陣インクの上を走っている間は消費と同時に回復も進みます

### CPU(ボット)のインク経済

CPUは従来どおりのインク経済のまま変更していません。CPUはインク管理が苦手、という前提の**ハンデ**として、人間より速く・無条件に回復します。

- 地形によるインク回復速度: **自陣の色の上+35/秒・それ以外(未塗り/相手色とも)+8/秒**
- 撃っている最中やため中でも回復は止まらない(遅延なし)
- 武器ごとの消費インクはCPU用の既定値(シューター1発6・ローラー振り34・チャージャー基礎15+チャージ割合×35・バケツ1発19)で、移動塗り(ローラー・フデ)は完全に無料

## アバター

各プレイヤーが自分のキャラを作れます。武器選択画面の各プレイヤー側に「アバター」ボタン(今のアバターの顔つきをそのまま表示)があり、押すとその側にキャラクリ画面がオーバーレイで開きます(P2側は他の要素と同じく180度回転)。両者が同時に開いても問題ありません。すべてCanvasで描画しており、画像ファイルは使っていません。

- **選べるパーツ**: しゅぞく(イカ/タコ)・かみがた(ショート/ロング/ツインテール/ポニーテール/おだんご/ボウズの6種)・かみのいろ(8色)・め(まる/たれ/つり/ほし/ねむいの5種)・はだ(6色)・ぼうし/アクセ(なし+キャップ/リボン/ヘッドホン/めがね/はな/ヘルメットの6種)・ふくのいろ(8色)・なまえ(最大6文字)
- 「ランダム」ボタンで全パーツを一括ランダム化、「OK」で確定して選択画面に戻る
- 選択内容はP1/P2別に`localStorage`(`sploteen_avatar_v1_p1` / `_p2`)へ保存され、次回起動時も同じアバターで始まる(保存に失敗しても対戦は続行できる)
- キャラクリ画面には正面向き上半身の大きなプレビュー(約160px角)を表示
- アバターの見た目は完全に演出のみで、`simulate()` のゲームロジックには一切影響しません

### ゲーム中のアバター表示(第7ラウンドで拡大 → 第11ラウンドで3D化)

- 対戦中のキャラは**3Dモデル**(全高約57px。あたり判定`PLAYER_RADIUS`は変えていません)。アバターの設定からその場で組み立てる(「[3D表示](#3d表示第11ラウンド)」参照)
- 2Dフォールバック時は従来どおり、キャラクリのプレビューと同じ正面向き上半身の絵を高さ約54pxで表示する(オフスクリーンcanvasに描いておいた絵を`drawImage`で貼るだけ)。P2のキャラは180度回転して描く
- 向いている方向(エイム)は3Dでは体の向きと足元の矢印で、2Dでは小さな三角の矢印で示す。武器は3Dでは手に持ったモデル、2Dでは絵文字の小アイコンで表示する
- 被弾フラッシュ・無敵中の点滅・こおり/はっこう/あぶりの状態異常アイコン・HPバーは、この新しい表示の上にそのまま乗っている
- HUDの武器アイコンの隣に、アバターの顔だけを描いた32px程度の丸いアイコンを表示(アバター変更時だけ再描画)
- カウントダウンの直前に1.2秒の「VS」画面を挟み、中央に「VS」、下にP1(なまえ・メイン武器名・アバター)、上にP2(180度回転、同様)を表示する
- ラウンド結果画面・優勝画面では、勝った側のアバターを大きく(約120px)・軽くはねるアニメーションつきで、負けた側は小さく横に表示する。ひきわけの場合は両方同じ大きさ。P2が勝った場合もその表示は180度回転してP2側から見える向きになる

## 共通ルール

- HP100。やられると3秒後に自分のスタート地点で復活、復活後1.5秒は無敵。頭上に小さなHPバー、自分側HUDにもHPバーを表示
- 復活時はHPだけでなく**インクも満タンに戻る**(第8ラウンドで追加。インク切れで倒された後、空のまま復活して何もできない状態を防ぐ)。状態異常も解除される
- スペシャルゲージは自分の色で新しく塗ったセル数でたまる(上の「スペシャル」参照)
- 3ラウンド制・2本先取。ラウンドに負けた側は次のラウンド開始時にスペシャルゲージ50%でスタート

## ボットと自動対戦API

`window.SPLOTEEN` に以下を公開しています(バランス調整・自動テスト用)。

```js
SPLOTEEN.WEAPONS        // メイン武器パラメータ。書き換えると次の対戦から反映される
SPLOTEEN.SUBS           // サブウェポンパラメータ
SPLOTEEN.SPECIALS       // スペシャルパラメータ(武器から独立)
SPLOTEEN.CONFIG         // { BOT_SKILL: 0〜1, ... } ボットの強さ・判断パラメータ
SPLOTEEN.simulate({ p1:'shooter', p2:'roller', sub1:'splashbomb', sub2:'quickbomb', sp1:'sensei', sp2:'manta', seed:1, duration:90, bot1:true, bot2:true })
  // 描画なしで1試合をヘッドレス実行。sub1/sub2・sp1/sp2省略時は各メインの既定サブ・既定スペシャルが使われる
  // { p1Pct, p2Pct, winner, splats, specials, kills, subs, statusHits, inkPainted } を返す
  // kills: [P1が倒した数, P2が倒した数] / subs: [P1のサブ使用回数, P2のサブ使用回数]
  // statusHits: [P1が状態異常を与えた回数, P2が状態異常を与えた回数]
SPLOTEEN.startMatch({ p1, p2, sub1, sub2, sp1, sp2, seed, bot1, bot2, duration })
  // 画面上でメニューをスキップして対戦を開始(sub1/sub2・sp1/sp2省略時は各メインの既定値)
SPLOTEEN.setInput(player, { move:{x,y}, aim:{x,y}, fire:bool, special:bool, sub:bool })
  // 外部から入力を強制注入(nullで解除)。sub:trueでサブウェポンを使用
SPLOTEEN.getState()      // 残り時間・各プレイヤーの状態(kills/sub/subsUsed/special/status含む)・塗り割合を取得
```

## URLパラメータ

URLパラメータでも直接対戦を開始できます。

```
index.html?p1=bucket&p2=brush&sub1=splashbomb&sub2=sprinkler&sp1=utsubo&sp2=manta&auto=1&bots=1&seed=123
```

- `p1` / `p2`: 使う武器(`shooter` / `roller` / `charger` / `bucket` / `brush`)
- `sub1` / `sub2`: 使うサブ(`splashbomb` / `quickbomb` / `sprinkler` / `shield`。省略時は各メインの既定サブ)
- `sp1` / `sp2`: 使うスペシャル(`sensei` / `utsubo` / `manta`。省略時は各メインの既定スペシャル)
- `auto=1`: タイトル・武器選択をスキップして直接カウントダウンへ
- `bots=1`: 両プレイヤーをボット操作にする。`bot2=1` ならP2だけボット(1人練習用)
- `seed`: 乱数シード
- `duration`: 1ラウンドの秒数(既定90。動作確認用に短縮できる)

ゲームロジックは1/60秒の固定タイムステップで決定論的に動作し、`Math.random` は使わず seed 付きの自前PRNG(mulberry32)のみを使用しています(同じseedなら常に同じ結果になることを確認済み)。

## 調整用の主なパラメータ

`index.html` 先頭にまとまっています。

- `CONFIG.BOT_SKILL`(既定0.6): ボットの強さ。反応の速さ・エイム精度に影響。CPUモードの「よわい/ふつう/つよい」ボタンがこれを0.35/0.6/0.85に切り替える
- `CONFIG.ADVANCE_BIAS`(既定0.6): ボットの塗り方向選択が相手の方向へ寄る強さ。会敵率(≒戦闘の起きやすさ)に直結する
- `CONFIG.SHOOTER_OUTRANGE_CHASE`(既定0.8): シューター・バケツのボットが、自分より射程の長い相手(チャージャー等)と対面したときに詰めるべきと判断する間合いを、相手の射程のこの割合まで引き上げる係数
- `BASE_SPEED` / `OWN_INK_MUL` / `ENEMY_INK_MUL`: 移動速度と地形補正
- `INK_REGEN_FAST` / `INK_REGEN_SLOW`: CPU用のインク回復速度(自陣上/それ以外)
- `INK_REGEN_OWN_HUMAN` / `INK_REGEN_UNPAINTED_HUMAN` / `INK_REGEN_ENEMY_HUMAN` / `INK_REGEN_DELAY`: 人間専用のインク回復速度と、消費後に回復が始まるまでの待ち時間
- `WEAPONS.*`: 各武器のダメージ・射程・塗り半径・移動速度・インク消費(CPU用/`human`内が人間用)など
- `SUBS.*`: 各サブの飛距離・ダメージ・インク消費など。`bot*` で始まるものはボットの運用方針(いつ・どれくらいの頻度で使うか)で、人間の操作には影響しません
  - `botThrowGap` / `botThrowGapSpan`: ボム系の「相手に向けた」再投げ待ち(秒)
  - `botPaintThrow` / `botPaintGap` / `botPaintGapSpan` / `botPaintMinCells` / `botPaintInkMargin`: ボム系を「塗るために」投げる判断(第11ラウンド)。着弾点で塗り直せるセル数が `botPaintMinCells` 以上、かつインクが `inkCost + botPaintInkMargin` 以上あるときだけ投げる
  - `botMinUnpainted`: スプリンクラーを置き先の「自分色でないセル数」で判断するしきい値
  - `botProactiveCells`: シールドを「撃たれる前に」先置きする相手との距離(セル)
- `SPECIALS.*` / `SPECIAL_GAUGE_MAX` / `SPECIAL_KILL_BONUS`: 各スペシャルのパラメータと共通ゲージ満タン値・倒した時の加算割合
- `SUB_COOLDOWN`(既定1.0秒): サブウェポン使用後の共通クールダウン
- `AVATAR_PARTS`: アバターの各パーツの選択肢一覧
- `WINS_NEEDED`(既定2): シリーズの先取本数
- `WALLS_CELL`: マップの壁配置(点対称)

3D表示(見た目だけに効くパラメータ。ゲームバランスには影響しません):

- `CAM_TILT`(既定42度): カメラを真上から何度倒すか。大きいほど寝かせた見下ろしになり、キャラの立体感が増すぶんマップの縦が詰まる
- `WALL_H`(既定1.5セル): 壁の高さ。高くすると迫力は出るが、奥(P2側)の床を隠す量が増える
- `CHAR_SCALE`(既定1.3): キャラモデルの表示倍率(あたり判定の`PLAYER_RADIUS`とは無関係)
- `INK_TILE_CELLS`(既定12) / `INK_TILE_RES`(既定1.5): 塗りテクスチャのタイル分割と解像度倍率。上げるときれいになるが毎フレームの転送が重くなる
- `STAGE_THICK`(既定6セル): 床下の土台の厚み(浮いているステージに見せるためのもの)

## 第11ラウンド: サブウェポンの強さをメインと比べて測り直した

「サブウェポンが弱すぎる」という指摘を受け、**サブの強さをメインと同じものさしで測る**ところから始めました。

### 「メインだけ vs サブだけ」で測る

第9ラウンドで使った「対サブなし勝率」は、**サブが効いているかどうか**は分かっても、**メインと比べてどれくらいの働きをしているか**は分かりません。そこで、同じメイン武器・同じサブを持った2体を、片方は**サブ禁止(メインだけ)**、もう片方は**メイン禁止(サブだけ)**にして戦わせ、**塗り面積の比**を測りました(スペシャルは両者オフ)。

| サブ | 調整前 サブ塗り% | 調整前 メイン塗り% | 調整前 塗り比 | 調整後 塗り比 |
| --- | ---: | ---: | ---: | ---: |
| スプラッシュボム | 3.5% | 74.4% | 0.047 | **0.187** |
| クイックボム | 2.9% | 77.0% | 0.038 | **0.114** |
| スプリンクラー | 1.3% | 74.2% | 0.018 | **0.095** |
| スプラッシュシールド | 0.7% | 68.1% | 0.011 | **0.128** |

サブだけの側の塗りは、メインだけの側の**1〜5%**しかありませんでした。指摘のとおり「弱すぎ」で、しかもその差は感覚ではなく数字で確認できるものでした。調整後は**2.5〜12倍**になっています(サブだけで勝てるようにはしていません。サブはあくまでメインを補う道具、という位置づけは変えていません)。

### 弱さの原因は3つあった

1. **1回ぶんの塗りが小さい。** 爆発半径・スプリンクラーの寿命と散布量・シールドの塗り帯のいずれも、1回使ってもマップの1〜2%しか塗れませんでした
2. **インクが重すぎた。** インク45〜50は、タンク100に対して半分近くです。特にシューター・バケツのように**撃つのにインクを使う武器**では、サブを1回使うぶんメインが撃てなくなり、武器×サブの相性行列では**サブを持つと「サブなし」より弱くなる**(シューター+スプラッシュボム 29.2%)ところまで損をしていました
3. **CPUがサブを塗りに使っていなかった。** ボムは「相手が飛距離内にいるとき」だけ投げる作りで、地面を塗る道具として一度も使われていませんでした

### 直した内容

パラメータ(人間の操作にも効く):

| サブ | 変更 |
| --- | --- |
| スプラッシュボム | 半径 4.5→4.9セル、インク **45→30** |
| クイックボム | 半径 3.3→3.8セル、インク **28→20** |
| スプリンクラー | 寿命 8→12.5秒、散布 毎秒4.5→6.5回、半径 5→5.8セル、飛沫 0.9→1.3セル、HP 30→50、インク **40→28** |
| スプラッシュシールド | 幅 6→8セル、厚み 0.9→1.0、持続 5→8秒、インク **50→35**、壁の下を塗る帯を厚みぶん(約1セル)から半径3.6セルに拡大 |

いちばん効いたのは**インクコスト**でした。シューターにスプラッシュボムを持たせたときの対サブなし勝率は、コスト40のままだと38.5%(＝持つと損)ですが、30にすると51.0%になります。一方、走るだけで塗れてインクが余っているフデでは97.9%のまま動きません。**サブを安くすると、インクを食う武器ほど得をする**ので、武器間のバランスを整える梃子としても使えました。

CPUの運用方針(`SUBS.*` の `bot*` パラメータ。人間の操作には影響しません):

- **塗り目的の投げを追加。** 飛距離ぶん先の8方向を見て「自分色でないセル」がいちばん多い方向へボムを投げる(`botPaintThrow`)
- **メインで狙える相手には投げない。** 壁越し・射程外など「メインでは届かない相手」にだけ対人の投げを行う。射程内の相手に投げると、撃つのをやめるぶんだけ損になっていた
- **スプリンクラーの置き先を、足元の未塗りセル数ではなく「置き先の自分色でないセル数」で選ぶ**ようにした(相手色の上にも置きに行ける)

### メイン武器のバランスへの影響も戻した

サブを強くすると、**インクを使わずに塗れるフデ**だけがその恩恵をまるごと受け取り、勝率が54.4%→64.1%まで上がってしまいました。逆にインクを食うシューターは下がります。そこでメイン側を2つだけ動かして戻しています。

- シューターの `paintRadius` 2.0→2.1
- フデの `trailWidth` 2.81→2.55

### 調整後の数字(48シードのCPU同士、90秒)

| サブ | 対サブなし勝率 | サブ間勝率 |
| --- | ---: | ---: |
| スプラッシュボム | 57.5% → **62.9%** | 55.6% → 55.3% |
| クイックボム | 57.5% → **65.0%** | 52.2% → 48.5% |
| スプリンクラー | 51.7% → **72.1%** | 58.9% → 49.8% |
| スプラッシュシールド | 55.0% → **65.2%** | 57.2% → 52.5% |

| メイン武器 | 調整前(P1側勝率) | 調整後(P1側勝率) |
| --- | ---: | ---: |
| シューター | 45.0% | 48.4% |
| ローラー | 45.6% | 46.9% |
| チャージャー | 55.0% | 57.3% |
| バケツ | 47.5% | 51.6% |
| フデ | 54.4% | 56.3% |

4種とも「持つ意味」が一段上がり(対サブなし勝率 51.7〜57.5% → 62.9〜72.1%)、サブ同士の均衡(48.5〜55.3%)とメイン武器のバランス(46.9〜57.3%、目標帯35〜65%)は調整前と同程度に保てています。

### 測り方(再現方法)

`simulate()` に**実験専用のフラグは足していません**。「メインだけ/サブだけ」は、`index.html` の `<script>` を抜き出してNodeの `vm` で走らせるときに、`firing` / `subPressed` / `specialPressed` を読む3か所を差し替えて実現しています(ゲーム本体のコードは通常どおり)。

## 第9ラウンド: 実験計画法による武器バランス調整

メイン・サブ・スペシャルの3軸を**それぞれ独立した実験**として切り分け、直交表(Plackett-Burman 12run)によるスクリーニングと確認実験を行いました。合計およそ35,000試合をヘッドレスで実行しています。

### 評価の分離設計

1つの軸を測るとき、他の2軸は「両プレイヤーで完全に同一」にして交絡を断ちます。

| 実験軸 | 両者で固定するもの | 変数 | 1条件あたり |
| --- | --- | --- | --- |
| メイン | 同じサブ・同じスペシャル(`splashbomb+sensei` と `quickbomb+manta` の2文脈で平均) | メイン武器 | 640〜1920試合 |
| サブ | 同じメイン(＝同じ既定スペシャルにもなる)。5メイン全部で平均 | サブ | 720〜2040試合 |
| スペシャル | 同じメイン・同じサブ。5メイン全部で平均 | スペシャル | 480〜960試合 |

いずれも総当たり×P1/P2入れ替え(先手バイアス除去)。目標は**各武器の通算勝率50%**で、**セル単位の相性(じゃんけん)は意図的に残しています**。

直交表には毎回**ダミー因子**(何にも割り付けない列)を1〜3本混ぜ、その見かけの主効果を誤差水準として読んでいます。メイン実験では約1pt、サブ実験では約1.2ptでした。

### 結果

| 軸 | 調整前の最大偏差 | 調整後の最大偏差 |
| --- | ---: | ---: |
| メイン武器 | 27.7pt | **1.8pt** |
| スペシャル | 22.5pt | **3.1pt** |
| サブ | 2.4pt | **2.2pt** |

| メイン武器 | 調整前 | 調整後 |
| --- | ---: | ---: |
| シューター | 68.0% | 48.8% |
| ローラー | 45.5% | 48.7% |
| チャージャー | 29.7% | 49.4% |
| バケツ | 77.7% | 51.3% |
| フデ | 29.1% | 51.8% |

| スペシャル | 調整前 | 調整後 |
| --- | ---: | ---: |
| せんせいストライク | 54.7% | 48.1% |
| むらさめウツボ | 27.5% | 53.1% |
| マンタりんぶ | 67.8% | 48.8% |

残っている相性の例(P1側勝率): バケツ→シューター68%、フデ→バケツ76%、チャージャー→フデ60%、シューター→バケツ32%。これらは潰さずに残しています。

### サブは「意味があるか」を別指標で測った

サブ同士の勝率だけを見ると4種とも50%付近でしたが、これは**4種とも等しく無意味でも同じ数字になる**ため、指標として不十分でした。そこで「インクコストが高すぎてボットが絶対に使えないダミーサブ」を用意し、**対「サブなし」勝率**と**1試合あたりの使用回数**を新しい応答として測りました。

| サブ | 対サブなし勝率(持つ意味) | サブ間勝率(均衡) | 使用回数 |
| --- | ---: | ---: | ---: |
| スプラッシュボム | 66.3% → 60.7% | 50.1% | 4.3 → 4.3回 |
| クイックボム | 58.8% → 54.0% | 51.9% | 4.4 → 6.9回 |
| スプリンクラー | 56.3% → 54.0% | 47.8% | 7.3 → 6.0回 |
| スプラッシュシールド | 52.5% → **56.7%** | 50.2% | **0.9 → 5.8回** |

この指標で、**スプラッシュシールドがほぼ無意味**(持っても勝率+2.5pt、1試合に1回も使われない)だったことが分かりました。原因はパラメータではなく**ボットの運用方針**で、「撃たれた直後」にしかシールドを張らなかったためです。パラメータをいくら盛っても勝率が動かなかったのはこのためでした。

同じく**クイックボム**も「れんとうできる」という説明に反して、再投げ待ちがスプラッシュボムと同じ5〜10秒に固定されていました。

そこでボットのサブ運用方針を `SUBS.*` 側のパラメータとして外に出しました(いずれも省略時は従来どおりの挙動)。

- `botThrowGap` / `botThrowGapSpan`: ボム系の再投げ待ち秒数(既定 5 + 0〜5)
- `botMinUnpainted`: スプリンクラーを設置する未塗りセル数のしきい値(既定4)
- `botProactiveCells`: シールドを「撃たれる前に」先置きする相手との距離(セル。既定0＝無効)

`botProactiveCells:10` は全サブ因子中で最大の主効果(+11.7pt)を示しました。

### 主な知見

- **ダメージ系のパラメータはほぼ効かない。** シューターの `damage`、バケツの `damage`、ローラーの `swingDamage`、マンタの `contactDamage` はいずれも誤差水準だった。勝敗が塗り面積で決まる以上、**塗り半径・塗り幅・連射速度・移動速度・スペシャルの塗り範囲**が支配的になる
- **チャージャーが弱かったのは火力不足ではなく「塗れなさ」。** `beamPaintRadius` 単体の主効果が +42.2pt/1セルと、全因子中で突出していた
- **スペシャルの偏り(22.5pt)はサブの偏り(2.4pt)の約10倍**あり、既定スペシャルの割り当てを通じて武器バランスに直撃していた。メインだけを均しても、既定構成でのバランスは均せない
- 勝率は**ゼロ和**(5武器なら合計250%で固定)なので、連立方程式はランクが1落ちる。リッジ付き最小ノルム最小二乗で解いた
- 主効果の線形モデルは局所的には正確だが、**大きく動かすと実際のゲインが2倍前後になる**ことがあった。1回で決めず、実測した2点間の割線補間で最終点を決めている

### 再現方法

`SPLOTEEN.simulate()` はブラウザAPIにほとんど依存しないため、`index.html` の `<script>` を抜き出して最小限のDOMスタブと一緒にNodeの `vm` で実行すれば、そのままヘッドレスで回せます(1試合あたり約75ms、4プロセス並列)。

```js
SPLOTEEN.simulate({ p1:'charger', p2:'bucket', sub1:'sprinkler', sub2:'shield',
                    sp1:'sensei', sp2:'utsubo', seed:1, duration:90 })
```

### 注意: これはCPU同士のバランス

上の数値はすべて `BOT_SKILL` 既定値(0.6)のボット同士の対戦です。人間が操作した場合のバランスは保証していません。特にサブとスペシャルは、ボットの使い方(いつ投げるか・いつ張るか)に結果が強く依存するため、人間の操作では違う結論になる可能性があります。

## インク経済の実験計画法による検討(参考)

CPUも人間と同じインクルールにするかどうかを検討する過程で、実験計画法を使った調整を行いました。

CPU も人間と同じインクルールにする案は、3 段階 48 条件の直交表実験で検討したが、異種 10 組中 5 組までしか 35〜65% に収まらなかったため、CPU は従来のインク経済のままとした。最良条件: 回復 35/秒・待ち時間 0.25 秒・シューター 1 発 4・チャージャー 4+8・バケツ 14・転がし 0.15/セル・走り 0.06/セル・当たり判定半径 11・連射規律なし・CPU の回復モードあり・チャージャー塗り幅 2.0・チャージャーため 0.9 秒/速度 1.15・フデ速度 1.35

この結果を踏まえ、CPUは引き続き第5ラウンドまでと同じインク経済(自陣+35/秒・それ以外+8/秒、消費中も回復停止なし)で動かし、人間プレイヤーにだけ上記の専用インクルール(自陣35/未塗り8/相手0・回復遅延0.5秒・武器別の軽いインクコスト)を適用する、現在の方式を採用しています。

## スペシャル発動のスクリーンショット

Playwrightで `?p1=<武器>&p2=<武器>&sp1=<スペシャル>&sp2=<スペシャル>&auto=1&bots=1` を820×1180で開き、`SPLOTEEN.getState()` の `specialActive` を監視して発動の瞬間を撮影しました(ゲージ満タン値を一時的に下げた検証用コピーで速く発動させています。ゲームロジック自体は本番の `index.html` と同一)。

- せんせいストライク: 赤い予告線とその後の直撃/こおり演出
- むらさめウツボ: 前方扇状に降り注ぐウツボの群れ
- マンタりんぶ: マンタに乗って接触キル(「たおした!」ポップアップ)

保存先(このリポジトリには含まれないスクリーンショット):

- `/tmp/claude-0/-home-user-game1/968f2ac7-6138-5e83-829a-16b429dd9bff/scratchpad/par-docs-sp-sensei.png`
- `/tmp/claude-0/-home-user-game1/968f2ac7-6138-5e83-829a-16b429dd9bff/scratchpad/par-docs-sp-utsubo.png`
- `/tmp/claude-0/-home-user-game1/968f2ac7-6138-5e83-829a-16b429dd9bff/scratchpad/par-docs-sp-manta.png`
